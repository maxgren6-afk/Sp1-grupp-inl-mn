using UnityEngine;

public class Testing : MonoBehaviour
{
    int nummer = 10;
    float decimalTal = 0.45f;
    string namn = "Sami";
    bool car = false;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print(nummer);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
